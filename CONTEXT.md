# NexusKit — 智能工具集

## 领域术语

### NexusKit
在线工具平台，提供多种实用工具，无需安装即开即用。

### 工具卡片
首页展示的功能入口，每个卡片代表一个工具。点击后跳转到对应工具页面。

### 透视网格
首页 Hero 区域的氛围背景层。淡蓝色细线网格铺满 Hero，中央内容区淡出避让；一条宽幅柔光带沿网格缓慢横扫，鼠标经过处网格线局部提亮。透视网格仅承担视觉氛围，不承载信息，不影响 Hero 内容可读性。

### 需求分析
NexusKit 的需求分析工具，取代原"智能助手"工具卡片入口。对用户上传的需求文档做测试视角的 AI 分析，产出需求分解树，并识别需求中的风险点、歧义点与待澄清问题。分析结果以图表形式可视化，并可导出到常见脑图工具：XMind（导出文件跟随当前选中的图表类型）、FreeMind（.mm）、Markdown 大纲与 PNG 图片。
需求分析中的"功能模块""功能点"是需求分解树的节点层级，与用例生成工具中的"功能"（生成任务的分组标签）不是同一概念。
分析结果持久化为分析记录：每次分析完成后自动保存，页面关闭后记录仍在，可回看、再导出、删除。导出文件是记录的交付物。
需求分析是用例生成的上游入口之一：分析完成后可将需求原文一键接力到用例生成工具，预填新建用例弹窗的需求描述；需求分解树与分析结论不进入用例生成管线。

### 分析记录
需求分析的历史持久化记录，对应测试用例侧的"项目列表 ↔ 用例集"结构中的列表层。分析记录在需求分析工具内以列表视图呈现，与结果视图构成"记录列表 ↔ 结果视图"两层结构。每条分析记录包含名称、需求原文、需求分解树、分析结论、当前图表类型与时间信息；打开记录后切换图表类型即更新记录。
记录名称自动产生：上传文件取文件名，粘贴文本取首行前 20 字，列表内可重命名。
只保存完成态：分析成功完成才自动入库；分析中途关闭页面或断流视为放弃，不产生记录。删除记录前需要轻量确认（对齐删除用例语义）。列表操作与测试用例集列表对齐：打开回看、再次导出、删除、分页。

### 需求分解树
需求分析的核心产出，是对需求文档的层级化分解：业务域 → 功能模块 → 功能点 → 业务规则/约束。需求分解树以分析结果图表可视化，并作为导出文件的载体；分析结论关联到树的节点，但不属于树的结构本身。

### 分析画板

需求分析工具中打开分析记录即进入的全屏结果工作区，是"记录列表 ↔ 结果视图"两层结构中结果视图的形态，对应路由为 `/requirement-analysis/board/:id`。分析画板支持自由缩放、平移与适应屏幕；已从只读阅览形态进化为可编辑的测试设计画布，画布内容持久化在分析记录的 `board` 字段。

### 测试设计画布

分析画板进化后的可编辑工作台形态，承载需求分解树参考图与测试设计方法图元。用户可在自由白板上插入因果图、判定表、正交表，经 AI 生成草稿、手动编辑与确定性推导后，将用例骨架接力到用例生成工具。

### 因果图

测试设计画布支持的图元类型，用于描述原因（条件）与结果（动作）之间的逻辑关系，节点间通过"与/或/非/恒等"约束边连接。因果图是"黑盒三件套"之一，需先转换为判定表才能导出用例骨架。

### 判定表

测试设计画布支持的表格型图元，由条件桩、动作桩与规则列组成，规则列用 `Y/N/-` 表达条件组合与对应的动作取值。判定表可直接导出用例骨架，也可由因果图经确定性推导生成。

### 正交表

测试设计画布支持的表格型图元，用于在多因子多水平组合爆炸时选取代表性组合。正交表由因子、水平与算法生成的阵列行组成，用户可编辑因子与水平后重新生成阵列，并直接导出用例骨架。

### 用例骨架

测试设计画布从判定表或正交表推导出的结构化测试用例雏形，每条骨架包含前置条件、操作步骤与预期结果三个要素。用例骨架序列化为文本后，通过 `REQUIREMENT_HANDOFF_KEY` 接力到用例生成工具，由现有 AI 管线补全为正式 8 列用例。

### 思考过程
推理型模型在正式回答前生成的推理链（reasoning_content）。需求分析在 AI 分析阶段实时展示思考过程；模型不提供思考过程时，退化为展示正文生成流（等宽字体、默认折叠）。思考过程只用于过程展示，不进入分析结果与导出文件。

### 分析结果图表
需求分析结果的可视化形态，是同一份需求分解树的不同渲染，包含三种：思维导图（脑图，默认）、树状图、逻辑图。用户在结果页可随时切换图表类型，切换只是更换渲染方式，不重新执行 AI 分析。

### 分析结论
需求分析在需求分解树之外识别出的三类发现：风险点、歧义点与待澄清问题（需求文档未说明、需要向需求方确认的问题）。分析结论不是分解树的节点，而是独立成组的发现：图上节点仅以角标提示关联的结论数量，完整结论在独立面板中按类型分组展示，并随导出文件一起交付（追加章节或节点备注）。

### 需求文档
需求分析工具的输入。支持 .md、.txt、.docx、.xlsx、.xls、.csv、文字型 .pdf 的单文件上传（不超过 10MB），或直接粘贴文本输入。不支持 .doc、图片与扫描件；解析后文本超长时按章节截断，并向用户明确提示截断范围。

### Jmeter脚本
NexusKit 的性能测试工具，基于 jmeter-mcp 服务。支持三种使用模式：
- **模板选择**：预设 8 种性能测试模板（6 常用 + 2 高级），用户填写参数后生成 .jmx 文件
- **AI 生成**：用户用自然语言描述测试需求，AI 调用 jmeter-mcp 自动生成测试计划
- **自定义脚本**：用户手动编写 Groovy/JS/Python 脚本

### AI 生成执行弹窗
Jmeter脚本工具中 AI 生成模式的过程型弹窗。用户点击“开始 AI 生成”后，前端立即打开 AI 生成执行弹窗，在同一个浮层内切换查看“执行过程”和“执行结果”两个步骤。

### AI 结果计划树
AI 生成执行弹窗中“执行结果”步骤里的 JMeter 计划树视图。AI 结果计划树不展示静态文本快照，而是绑定到本次 AI 生成后后端当前内存态的测试计划。
用户在 AI 结果计划树中的编辑、删除、移动和脚本替换，都直接作用于当前测试计划，并可再次保存为 `.jmx` 文件。

### jmeter-mcp
Java 实现的 MCP 服务，暴露 57 个 JMeter 工具，支持创建、编辑、验证、执行 JMeter 测试计划。
- 支持 12 种采样器：HTTP、JDBC、TCP、FTP、JMS、SMTP、LDAP、Mail Reader、System、JSR223、BeanShell、Test Action
- 支持 16 种控制器、13 种断言、12 种监听器、7 种定时器、12 种配置元素
- 输出标准 .jmx 文件，兼容 JMeter GUI

### 模型配置
用户自配的统一供应商信息，存储在浏览器本地（`localStorage`），不上传后端。
统一供应商用于描述一套单供应商、单模型的 AI 接入配置，并支持保存多条后切换当前使用项。
统一供应商不表达“应用启用/停用”语义，也不要求用户分别维护 Claude、Codex、Gemini 多组模型字段；系统会根据配置中的 API 格式自动适配到对应请求协议。

### 项目
用例生成工具中的业务系统/产品容器。一个项目对应一个独立的业务产品或系统（如“用户中心”“订单系统”），项目下包含多个测试用例集。
项目是用户进入用例生成工具后的第一层视图。

### 测试用例集
项目下持久化保存的一组相关测试用例。用户发起一次新的 AI 生成时，系统先创建测试用例集，再为该用例集关联一个生成任务。
测试用例集在列表中展示关联生成任务的当前状态；生成完成后，可进入阅览、导出或删除。测试用例不会因生成任务结束而脱离所属测试用例集。
测试用例集使用“用例集名称”作为唯一面向用户的名称；该名称同时作为 AI 生成时的功能模块名称，不再向用户展示独立的“功能名称”字段。

### 功能
生成任务上的需求标签，用于在项目内对任务进行分组（如“登录功能”“下单功能”）。功能以 `featureName` 标识，是任务列表的“按功能区分”维度，不是独立的容器或持久化实体。
同一项目下可以存在多个相同功能的生成任务，每个任务保留自己的用例结果。

### 生成任务
用例生成工具中一次 AI 生成请求的完整生命周期记录。每个生成任务包含需求描述、生成参数、执行状态和最终解析出的用例结果。
生成任务从属于一个测试用例集，用于表达 AI 生成过程及其状态，不作为项目内独立展示的业务容器。生成完成后的有效用例持久化到所属测试用例集。
失败状态下支持重试。重试会复用同一任务 ID，清空之前的错误和中间结果后重新执行。

### 补充需求
补充需求是对已生成完成的测试用例集追加新测试范围的 AI 二次生成操作。用户输入本次要补充的需求说明，系统基于当前用例集已有用例和补充说明生成新增用例，并在生成完成后直接追加到原用例集末尾。
补充需求不创建新的测试用例集，也不覆盖已有用例；追加后用例编号由后端重新连续编号。

### 新建用例弹窗
用例生成工具中新建测试用例集的创建入口。用户在弹窗中填写用例集名称、测试类型、输出语言、覆盖模式、最大条数上限和需求描述；提交后创建测试用例集并开始关联的 AI 生成任务。

### 新增用例
新增用例是用户在已有测试用例集中手动创建单条测试用例的维护操作。新增用例不调用 AI，不创建生成任务，保存后直接写入当前测试用例集并参与导出。
新增用例继续使用固定 8 列结构；用例编号由后端按当前用例集顺序重新连续维护。

### 删除用例
删除用例是用户从已有测试用例集中移除单条测试用例的维护操作。删除前需要轻量确认；确认后该用例从当前测试用例集中移除，并由后端重新连续维护用例编号。
本期删除用例不进入回收站，也不提供撤销。

### 用例列表
测试用例集中的测试用例维护视图。用例列表用于阅览、手动新增、删除、导出和承载补充需求后的新增用例；列表中的每条用例持久化归属于当前测试用例集。

### 用例维护弹窗
用例维护弹窗是已完成测试用例集的当前维护入口，由原测试用例阅览窗口升级而来。用户点击已完成用例集后，在弹窗中阅览用例、导出结果、发起补充需求、手动新增用例和删除已有用例。
用例维护弹窗是本期过渡形态，不改变测试用例集作为用例归属容器的语义。

### 用例集合并导出
项目内测试用例集列表提供的批量导出能力。勾选一个用例集时导出该用例集，勾选多个时合并导出所选用例集；未勾选时导出当前项目下全部已生成完成的用例集。
生成中或生成失败的测试用例集不进入导出内容。测试用例阅览窗口中的导出始终只作用于当前阅览的测试用例集。

### 用例编号
用例列表中每条用例的编号。用例编号由后端在解析完成后统一重排，不依赖 AI 输出。
功能测试使用 `TC001, TC002, TC003...`；API 测试使用 `API-TC001, API-TC002...`。

### 用例质量校验
用例生成工具内部的结果校验机制。用例质量校验用于过滤或修复不符合要求的生成结果，包括列数、用例编号、步骤完整性、预期结果、优先级、标题重复和解释性文本等问题。
当生成结果不合格时，后端可以自动发起一次修复调用；修复后仍不合格时，保留有效用例并记录质量提示。
用例质量校验结果不在前端展示，前端只展示任务状态和有效用例结果。

### 报告展示风格
测试报告页面中的视觉主题。报告展示风格用于切换颜色、背景、卡片表面、标签、按钮和图表皮肤，不改变报告数据本身的含义。

### 测试报告视图
测试报告页面中的唯一展示方案。测试报告视图用于承载质量摘要、趋势图表和风险判断，不再向用户提供多套布局方式选择。

### 报告图表显示窗口
测试报告图表中的默认可见数据范围。报告图表显示窗口用于限制图表首屏展示的数据数量，并通过卡片内滚动继续查看剩余数据，不丢弃原始统计结果。

### 报告图表排序规则
测试报告图表中的默认数据排列语义。报告图表排序规则用于决定“前 N 条”与滚动列表的展示顺序，必须与图表标题表达的分析目标一致。

### 覆盖模式
用例生成工具中的生成策略。覆盖模式用于表达希望 AI 达到的测试覆盖深度，优先级高于固定生成条数。
覆盖模式分为三档：
- **快速覆盖**：覆盖主流程、关键失败场景和基础边界。
- **标准覆盖**：默认模式，覆盖正向、反向、边界、权限、状态、数据校验和异常提示。
- **专家覆盖**：覆盖标准模式内容，并增加组合场景、风险场景、历史缺陷高发点、安全、兼容性和幂等性等扩展维度。
不同覆盖模式对应不同生成深度：
- **快速覆盖**：一次模型调用，速度优先。
- **标准覆盖**：两次模型调用，先做内部需求分析，再生成用例。
- **专家覆盖**：三次模型调用，先做内部需求分析，再生成用例，最后做覆盖自检补全。
覆盖维度不新增为用例列表字段，应体现在“功能测试点”或“用例标题”中，以保持当前 8 列格式和导出格式稳定。

### 最大条数上限
用例生成工具中的数量约束。最大条数上限用于控制生成规模，不要求 AI 必须凑满该数量。
最大条数上限是硬上限，生成结果不能超过该数量；AI 可以少于上限，但不能为了凑满数量牺牲用例质量。
默认上限：
- **快速覆盖**：8 条。
- **标准覆盖**：20 条。
- **专家覆盖**：40 条。
当覆盖维度超过最大条数上限时，优先保留核心主流程、高风险异常、边界、权限/状态、组合场景、兼容/可用性等更高价值场景。

### 性能测试模板
预设的 JMeter 测试计划骨架，用户只需填写关键参数即可生成 .jmx 文件。
- 常用模板：API 压力测试(HTTP)、数据库性能测试(JDBC)、TCP 连接测试、邮件发送测试(SMTP)、文件传输测试(FTP)、LDAP 目录测试
- 高级模板：自定义脚本(JSR223)、系统命令测试(System)
- 空白模板：从零开始自定义

### HTTP 请求类型
JMeter HTTP 请求支持多种请求类型，每种类型有不同的参数格式：
- **Query 参数**：GET/HEAD/DELETE 等，参数通过 URL Query 传递
- **JSON Body**：POST/PUT/PATCH，Content-Type: application/json
- **Form 表单**：POST，Content-Type: application/x-www-form-urlencoded
- **Multipart**：POST，Content-Type: multipart/form-data，支持文件上传
- **XML Body**：POST/PUT，Content-Type: application/xml
- **Raw Body**：POST/PUT，Content-Type: text/plain

Content-Type 通过 HTTP Header Manager 设置，不在 HTTP Request 元素中。

### 参数编辑器
- **CodeMirror JSON 编辑器**：自动格式化 + JSON 校验 + 语法高亮 + 折叠
- **高级参数表格**：key-value 表格 + 增删行 + 启用/禁用 + 编码选项
- **XML/Text 编辑器**：基础语法高亮

### HTTP 方法
支持 21 种：GET, POST, HEAD, PUT, OPTIONS, TRACE, DELETE, PATCH, PROPFIND, PROPPATCH, MKCOL, COPY, MOVE, LOCK, UNLOCK, REPORT, MKCALENDAR, SEARCH

### Trace Span
后端日志系统的核心追踪单元。每个 HTTP 请求生成一个根 span，内部子操作（AI 调用、Store 写入、Export 生成、JMeter 工具调用）生成子 span，通过 traceId 串联成树状链路。span 记录名称、分类、耗时、状态和业务属性，用于请求级排查。

### TraceId
请求级唯一标识符，由 middleware 在请求入口生成，通过 AsyncLocalStorage 自动传播到该请求的所有异步子操作。开发者通过 traceId 查询完整请求链路。

### AppError
统一的应用错误类型，替代原先分散的字符串错误、JSON 错误、异常抛出三种模式。包含 code、message、httpStatus、cause 字段，由全局 error middleware 捕获并关联到 trace span。

## 架构决策

- 前端：React + TypeScript + TailwindCSS（Vite）
- 后端：Node.js + Express + TypeScript
- AI 调用：SSE 流式输出，实时展示 AI 调用 MCP 工具的过程
- 配置存储：浏览器 `localStorage`，用户自管
- 日志与追踪：结构化 trace 日志，AsyncLocalStorage 传递上下文，开发 console 美化 + 生产文件 JSON 输出
