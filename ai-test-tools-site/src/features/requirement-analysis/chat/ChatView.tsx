/**
 * 会话视图占位组件（T12 替换）。
 */
export function ChatView() {
  return (
    <div className="flex h-full items-center justify-center text-muted">
      <div className="text-center">
        <p className="text-lg font-medium">会话视图</p>
        <p className="mt-2 text-sm">T12 将在这里替换为消息流与文件卡片。</p>
      </div>
    </div>
  )
}
